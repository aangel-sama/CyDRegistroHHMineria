// Archivo de configuración de Next.js
// Aquí se pueden añadir opciones de build o runtime.
// Tipado opcional para que el objeto de configuración tenga autocompletado
import type { NextConfig } from "next";

// Objeto con las opciones de Next.js. Actualmente no hay configuración
// personalizada, pero se deja preparado por si se necesitan futuras opciones.
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
