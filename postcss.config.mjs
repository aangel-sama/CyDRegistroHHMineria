// Configuración mínima de PostCSS utilizada por Next.js
// Aquí se pueden añadir plugins adicionales si es necesario, por ejemplo
// autoprefixer o TailwindCSS.
const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;
